(function () {

    'use strict';

    /* Somente as dependencias do painel do usuario */
    var dependencies = [
        'ngRoute'
    ];

    angular
        .module('time02painel', dependencies);

})();